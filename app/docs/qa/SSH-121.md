# SSH-121 QA Proof
- Feature: Update Connection Failed Error Message for Exhausted Authentication Methods
- `SshService.kt` catches the `Exception` from the network layer. If the raw message contains "Exhausted available authentication methods" or "All authentication methods failed", it intercepts and remaps the string.
- The new message is `"Authentication exhausted. Please check your credentials."` which provides a much clearer explanation of why the connection was rejected.
- This text is safely bubbled up via `ConnectionStateRepository` and rendered exactly as written in `TerminalScreen.kt` via an `AlertDialog`.
- The test suite `./gradlew testReleaseUnitTest` passed successfully, proving no assertions were broken and no NullPointerExceptions occur during the interception.