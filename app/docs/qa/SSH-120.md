# SSH-120 QA Proof
- Feature: Allow renaming of the default Uncategorized group
- `SettingsManager.kt` was updated to persist a string preference for `defaultGroupName`, defaulting to "Uncategorized".
- `SettingsScreen.kt` was modified to include a "General Settings" card featuring an `OutlinedTextField` that allows the user to rename this fallback group dynamically. It falls back to "Uncategorized" if the user types a blank string.
- `GroupedConnectionList.kt` and `MoveToFolderBottomSheet.kt` have been refactored to read from `SettingsManager(context).defaultGroupName` rather than hardcoding the string literal "Uncategorized".
- The `ConnectionProfile` data model logic was untouched, as `folderId == null` implicitly routes to the fallback string at render time.
- All unit and visual regression tests executed successfully (`./gradlew recordPaparazziRelease && ./gradlew testReleaseUnitTest`), generating new screenshots of the `SettingsScreen` and confirming list behaviors.