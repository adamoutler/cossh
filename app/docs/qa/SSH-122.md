# SSH-122 QA Proof
- Feature: Telnet Protocol Support & Security Warnings
- `Protocol` enum was added to `ConnectionProfile.kt` and `AddEditProfileScreen.kt` was redesigned to support changing the protocol to SSH or Telnet.
- When `Telnet` is selected, an amber warning banner is displayed: "⚠️ Warning: Insecure Protocol."
- In `ConnectionItem.kt`, the SSH item has a green locked padlock, and the Telnet item has an orange open padlock. The subtitle displays `(SSH)` or `(Telnet)`.
- In `TerminalScreen.kt`, the `TerminalOverlayButtons` displays an amber `Warning` icon and "Telnet: Unencrypted Connection" when connected to a Telnet profile.
- Screenshots demonstrating these states have been automatically saved to `app/src/test/snapshots/images/` via `AddEditProfileScreenScreenshotTest`, `TerminalScreenOverlayScreenshotTest`, and `UserJourneyIntegrationTest` (which passed successfully).
- Verification passed via `./gradlew testReleaseUnitTest`. All assertions met.