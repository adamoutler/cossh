# SSH-125 QA Proof
- Feature: Redesign Port Forwarding UX to visual table format
- `AddEditProfileScreen.kt` was entirely rebuilt to use a declarative table layout using `Card` and `Row` for each `PortForwardConfig` item.
- `LazyColumn` logic was refactored to `Column` with `verticalScroll` to ensure complete UI tree composition for testing.
- An "Add Port Forward" button displays a Material `AlertDialog` using `SingleChoiceSegmentedButtonRow` for selecting "Local" vs "Remote" routing.
- The `ConnectionProfile` stores an actual `List<PortForwardConfig>` and maps it properly through the ViewModel.
- Screenshots are saved via Paparazzi to `app/src/test/snapshots/images/`.
- Unit tests and User Journey Integration tests all passed via `./gradlew testReleaseUnitTest` successfully, proving that no components were broken and port forwarding state is properly retained and handled.