# Verification Proof for SSH-126: Terminal Buttons Repeat & Ctrl-C

## Objective
Fix terminal buttons not repeating and add an explicit Ctrl-C button.

## Proof 1: Visual and Code Artifacts
The following screenshot artifact has been generated natively and is committed to the repository:
- `com.adamoutler.ssh.ui.components_TerminalExtraKeysScreenshotTest_page1_noModifiers.png`: Shows the Extra Keys layout updated to include a `Ctrl-C` button instead of `Super`.
Code proof confirms that `RepeatingClickable` is applied to repeatable keys (arrows, PgUp, PgDn, Ctrl-C) to allow them to repeat continuously when held down.

## Proof 2: CI Validation
The Dash Build Receipt confirms that the standard CI pipeline passed successfully, proving the new modifiers don't break existing behavior.
