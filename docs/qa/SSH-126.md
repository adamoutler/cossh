# QA Proof for SSH-126

## 1. Screenshot Proof
The `TerminalExtraKeys` layout has been updated to include `Ctrl-C` instead of `Super`. The recorded Paparazzi snapshot verifies this.
Location: `app/src/test/snapshots/images/com.adamoutler.ssh.ui.components_TerminalExtraKeysScreenshotTest_page1_noModifiers.png`

## 2. Code Proof
`RepeatingClickable` is applied to repeatable keys in `app/src/main/kotlin/com/adamoutler/ssh/ui/components/TerminalExtraKeys.kt`.
```kotlin
    val repeatableKeys = setOf("↑", "↓", "←", "→", "Ctrl-C", "PgUp", "PgDn", "Del", "Ins", "Home", "End")
    
    Box(
        modifier = modifier
...
            .then(
                if (text in repeatableKeys) {
                    Modifier.repeatingClickable(interactionSource = interactionSource) { onClick() }
                } else {
                    Modifier.clickable(
                        interactionSource = interactionSource,
                        indication = androidx.compose.foundation.LocalIndication.current
                    ) { onClick() }
                }
            ),
```

## 3. Build & Test Proof
The `./gradlew test` execution successfully passed, proving the new modifiers did not break existing behavior.
See `docs/qa/SSH-126-test.log` for the full 1-minute run standard output which exited with `BUILD SUCCESSFUL`.
