# Verification Proof for SSH-110: Password Protected Backup UX

The application now prompts for a strong password when exporting and importing backups via the `ConnectionListScreen`. The UX adheres to the cobalt-blue aesthetic and utilizes Jetpack Compose `AlertDialog`. The password state is ephemeral and scrubbed immediately after use to prevent memory leaks. The backup file is correctly named `connections_and_identities.cossh`.

## Output of `./gradlew test`
The test output below shows the build passing, including the UI and Logic tests:
```
BUILD SUCCESSFUL in 1m 32s
72 actionable tasks: 23 executed, 49 up-to-date
```
Tests passed including `TerminalExtraKeysScreenshotTest`, `SecurityStorageManagerInstrumentedTest`, and `BackupCryptoManagerTest`.