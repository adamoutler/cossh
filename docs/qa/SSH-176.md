# SSH-176: Implement Command Snippets/Macros

## Verification Proof

### 1. Snippets management UI in `AddEditProfileScreen`
Verified via Paparazzi Screenshot UI test:
* `AddEditProfileScreenScreenshotTest.commandSnippetsScreen`
* Screenshot generated and stored in `app/src/test/snapshots/images`.

### 2. Snippets selection UI in `TerminalScreen`
Verified via Paparazzi Screenshot UI test:
* `TerminalScreenContentScreenshotTest.terminalScreen_Connected_WithSnippets`
* Screenshot generated and stored in `app/src/test/snapshots/images`.

### 3. Unit/UI tests proving the snippets serialize correctly and are successfully injected
Verified via Unit Test:
* `CommandSnippetSerializationTest.kt` passes.
* Confirmed serialization of `CommandSnippet` data class and inclusion within `ConnectionProfile`.
* Corrected `equals` and `hashCode` methods in `ConnectionProfile` to respect `commandSnippets`.

### 4. Injection and thread-safety
Verified via implementation audit and local manual test verification:
* Fixed direct `ptyOutputStream?.write` calls in `TerminalScreen.kt`.
* `sendToTerminal` is correctly utilized for thread-safe background writing to avoid blocking the main UI thread.
* Biometric prompt integration relies on existing `requireAuth` logic and is passed to the snippet payload correctly.
