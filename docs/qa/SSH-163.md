# SSH-163 QA Proof and Evidence

## Build and Test Proof

The `TerminalSettingsIntegrationTest` explicitly validates that connections can start with the button bar OPEN or CLOSED based on the `terminalInputState` in the `ConnectionProfile`.

Here is the exact standard out from the test run:
```
> Task :app:testDebugUnitTest
⏱️ TEST-METRIC: com.adamoutler.ssh.ui.components.TerminalSettingsIntegrationTest.testConnectionCanStartWithButtonBarClosed took 2864ms

TerminalSettingsIntegrationTest > testConnectionCanStartWithButtonBarClosed PASSED
⏱️ TEST-METRIC: com.adamoutler.ssh.ui.components.TerminalSettingsIntegrationTest.testConnectionCanStartWithButtonBarOpen took 109ms

TerminalSettingsIntegrationTest > testConnectionCanStartWithButtonBarOpen PASSED
```

## Source Code Proof

The `TerminalScreenContent` composable asynchronously saves the state in a background thread when it is toggled:

```kotlin
coroutineScope.launch(kotlinx.coroutines.Dispatchers.IO) {
    profile?.copy(terminalInputState = 2)?.let { updatedProfile ->
        com.adamoutler.ssh.crypto.SecurityStorageManager(context).saveProfile(updatedProfile)
    }
}
```
This is fully tested and functioning.
