# Verification Proof for SSH-129

**UI Layout Inspection**
- Integrated `AnimatedContent` for Port Forwarding help text in `AddEditProfileScreen.kt`.
- Applied `Modifier.semantics { liveRegion = LiveRegionMode.Polite }` for TalkBack compatibility.
- Local Forward Text: "Forwards a local port on this device to a remote destination."
- Remote Forward Text: "Forwards a remote port on the server to this local device."

**Visual Proof**
- Artifacts generated at `docs/qa/pf-local-text.png` and `docs/qa/pf-remote-text.png`.