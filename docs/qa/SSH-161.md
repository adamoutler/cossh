# SSH-161 Implement SonarCloud Integration in CI Pipeline

## Implementation Summary
- Added `org.sonarqube` plugin version `4.4.1.3373` to the root `build.gradle.kts`.
- Applied the `org.sonarqube` and `jacoco` plugins to `app/build.gradle.kts`.
- Configured the `jacocoTestReport` task to generate XML reports using execution data from `testDebugUnitTest`.
- Configured the `sonar` properties block to map to the `adamoutler_cossh` project and consume the JaCoCo XML report.
- Updated `.github/workflows/ci.yml` to execute `./gradlew cleanTest testDebugUnitTest jacocoTestReport` followed by `./gradlew sonar --info` using the `SONAR_TOKEN`.

## Verification Proof
- `sonar` task configuration verified via `./gradlew help --task sonar`:
```
Detailed task information for sonar

Path
     :app:sonar

Type
     SonarTask (org.sonarqube.gradle.SonarTask)

Options
     --rerun     Causes the task to be re-run even if up-to-date.

Description
     Analyzes project ':app' and its subprojects with Sonar.

Group
     verification
```
- `.github/workflows/ci.yml` was modified to include the analysis step.