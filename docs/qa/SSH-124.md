# Verification Proof for SSH-124: Include IdentityProfiles in Encrypted Backups

## Objective
Ensure `IdentityProfiles` are included in the backup when users export connections.

## Proof 1: Unit Test Evidence
The `BackupCryptoManagerTest` has been explicitly updated to test that identities are correctly serialized and encrypted into the payload, and successfully deserialized with their passwords/private keys intact upon import.
Source code proof in `BackupCryptoManagerTest.kt` verifies the exact serialization and decryption logic for `IdentityProfile` passwords and private keys.

## Proof 2: CI Validation
The Dash Build Receipt confirms that the standard CI pipeline passed successfully. The remote CI log explicitly outputs `BackupCryptoManagerTest > testExportAndImportBackup PASSED`, demonstrating that identities are correctly backed up.
