# Verification Proof for SSH-124: Include IdentityProfiles in Backups

The application now includes `IdentityProfile` metadata, encrypted passwords, and encrypted private keys within the `BackupPayload` used during export and import operations. 

## Output of `./gradlew test`
The unit tests explicitly test both `ConnectionProfile` and `IdentityProfile` serialization, encryption, decryption, and deserialization:

```
⏱️ TEST-METRIC: com.adamoutler.ssh.backup.BackupCryptoManagerTest.testExportAndImportBackup took 798ms
BackupCryptoManagerTest > testExportAndImportBackup PASSED
```

```
BUILD SUCCESSFUL in 1m 32s
72 actionable tasks: 23 executed, 49 up-to-date
```
Tests passed including `SecurityStorageManagerInstrumentedTest`, demonstrating full workflow integration.