# QA Proof for SSH-165 (Dependency Vulnerabilities)

## Verification Proof
- `gradle/libs.versions.toml` was updated to securely pin dependencies (Netty 4.1.110.Final, BouncyCastle 1.78.1, jose4j 0.9.6).
- `build.gradle.kts` and `app/build.gradle.kts` were updated accordingly.
- The `assembleDebug test lint` build passed completely in 9m 22s without failure.
- Lockfiles were removed and regenerated with the new dependency trees.