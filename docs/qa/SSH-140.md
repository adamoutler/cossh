# Verification Proof for SSH-140: Telnet Backend Logic\n\nAdded `commons-net` dependency and branched connection logic inside `SshConnectionManager` to use `TelnetClient` when `profile.protocol == Protocol.TELNET`. Deployed directly to test device (192.168.1.39) for immediate manual verification.
