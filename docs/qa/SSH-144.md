# SSH-144 QA Proof

**User Story:** As a user, I want the terminal to support `xterm-256color` so that CLI tools like Gemini CLI display colors correctly.

**Verification Proof:**
1. Code change in `app/src/main/kotlin/com/adamoutler/ssh/network/SshConnectionManager.kt` explicitly sets the PTY allocation to use `xterm-256color` instead of the default `xterm`.
2. Automated standard unit tests run successfully.
3. Code is pushed and CI pipeline has passed, verifying no regressions were introduced.