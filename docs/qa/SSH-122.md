# Verification Proof for SSH-122: Telnet Protocol Support & Security Warnings

## Objective
Implement Telnet protocol support with clear UX warnings about its insecurity.

## Proof 1: Visual Artifacts
The following screenshot artifacts have been generated natively and are committed to the repository in the `app/src/test/snapshots/images/` directory:
- `com.adamoutler.ssh.ui.screens_AddEditProfileScreenScreenshotTest_telnetScreenWarningBanner.png`: Shows the `AddEditProfileScreen` with the Telnet warning banner.
- `com.adamoutler.ssh.ui.components_TerminalScreenOverlayScreenshotTest_overlayButtonsVisibleTelnet.png`: Shows the `TerminalScreen` with an active Telnet session and the header warning.
- `com.adamoutler.ssh.ui.screens_ConnectionListScreenScreenshotTest_defaultScreen.png`: Shows the `ConnectionListScreen` with distinct icons for SSH and Telnet profiles.

## Proof 2: CI Validation
The Dash Build Receipt confirms that the standard CI pipeline passed successfully, validating the new protocol serialization and deserialization without breaking existing encrypted backups.
