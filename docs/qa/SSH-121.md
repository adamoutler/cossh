# Verification Proof for SSH-121: Update Connection Failed Error Message

## Objective
Update the connection failed error message for exhausted authentication methods so that users are not confused by technical jargon.

## Proof 1: Visual Artifacts
The following screenshot artifact has been generated natively and is committed to the repository in the `app/src/test/snapshots/images/` directory:
- `com.adamoutler.ssh.ui.components_TerminalScreenDialogScreenshotTest_connectionFailedDialogScreen.png`: Demonstrates the specific UI state showing the new, clear error message "Connection failed" when an SSH connection fails due to authentication issues.

## Proof 2: CI Validation
The Dash Build Receipt confirms that the standard CI pipeline passed successfully, compiling the app and validating the newly introduced component logic. The logcat trace and unit tests confirm the correct error message is thrown.
