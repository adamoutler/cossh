# QA Proof for SSH-121

**Ticket:** SSH-121 Update Connection Failed Error Message for Exhausted Authentication Methods

## Validation Criteria Met:
1. **Screenshot Artifact:** The `connectionFailedDialogScreen` test was updated in `TerminalScreenDialogScreenshotTest.kt` to ensure the new "Connection failed" message is displayed instead of the exhausted methods jargon. The Paparazzi artifact visually demonstrating the change is located at:
   `app/src/test/snapshots/images/com.adamoutler.ssh.ui.components_TerminalScreenDialogScreenshotTest_connectionFailedDialogScreen.png`
2. **Unit Test Output:** The `SshServiceErrorMappingTest.testAuthenticationExhaustedMapping` was updated and executed successfully, explicitly proving the `mapExceptionMessage` function maps the "Exhausted available authentication methods" exception properly.

## Test Output Proof:
```
> Task :app:testDebugUnitTest
SshServiceErrorMappingTest > testAuthenticationExhaustedMapping PASSED
BUILD SUCCESSFUL in 6s
```
