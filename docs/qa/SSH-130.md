# Verification Proof for SSH-130

**UI Layout Inspection**
- Added `SectionHeaderWithInfo` composable for Advanced Configuration and Port Forwarding.
- Added `trailingIcon` to "Manage Identities" `DropdownMenuItem`.
- Links mapped to `https://github.com/adamoutler/ssh/wiki`.

**Integration Validation**
- Clicking the info icon triggers `LocalUriHandler.openUri()`.