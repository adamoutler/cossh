# SSH-175: Implement Dynamic Port Forwarding (SOCKS Proxy)

## Verification Proof

### 1. Screenshot of the Port Forwarding Dialog with the `Dynamic` option
Verified via Paparazzi Screenshot UI test:
* `AddEditProfileScreenScreenshotTest.portForwardDynamicDialogScreen`
* Screenshot generated and stored in `app/src/test/snapshots/images/`.
* The warning card `⚠️ Warning: This turns your device into a proxy...` is visible.
* Remote Host and Remote Port fields are successfully hidden.

### 2. Unit/UI tests proving the data model serializes the new `DYNAMIC` type
Verified via existing Unit Tests:
* `PortForwardType` enum extended to include `DYNAMIC`.
* Validated successfully within `DataSerializersTest.test PortForwardConfig serialization and deserialization` and `SerializerCoverageTest.testPortForwardConfigSerializationCoverage`.
* Tested via local `testDebugUnitTest` task run.

### 3. Build log proving the `PortForwardingOrchestrator` compiles
Verified via execution and inspection:
* `PortForwardingOrchestrator.kt` was successfully updated to evaluate `if (config.type == PortForwardType.DYNAMIC)` and call `startDynamicPortForward`.
* SOCKS5 handshake processing implemented in `handleSocks5Connection` using `DataInputStream`.
* The latest GitHub Actions CI pipeline passes successfully, and local builds via `./gradlew build` produce `BUILD SUCCESSFUL`.
